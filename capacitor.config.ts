import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.andii.petakeluarga',
  appName: 'Peta Keluarga',
  webDir: 'out',
  server: {
    androidScheme: 'https'
  }
};

export default config;
